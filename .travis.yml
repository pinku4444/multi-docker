sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t pinkukumar/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true pinkukumar/react-test npm test

after_success:
  - docker build -t pinkukumar/multi-client ./client
  - docker build -t pinkukumar/multi-nginx ./nginx
  - docker build -t pinkukumar/multi-server ./server
  - docker build -t pinkukumar/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push pinkukumar/multi-client
  - docker push pinkukumar/multi-nginx
  - docker push pinkukumar/multi-server
  - docker push pinkukumar/multi-worker

deploy:
  provider: elasticbeanstalk
  region: 'ap-south-1'
  app: 'multi-docker'
  env: 'Multidocker-env'
  bucket_name: 'elasticbeanstalk-ap-south-1-207638326760'
  bucket_path: 'multi-docker'
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY